ESP:
	0. На есп дожна стоять прошивка NodeMCU.
	1. В скрипте serv.lua заменить SSID и пасс на нужные. (да, нужен вифи-роутер с заранее известными именем и пассом).
	2. Переименовать этот скрипт в init.lua и закинуть на есп. (init.lua запускается по умолчанию, любые другие надо запускать самому)

Arduino:
	1. Прошить новой прошивкой. (D1_http.ino)
	
Всё:
	1. Собрать до кучи всё это. (соединить по уарту ардуину и есп)
	2. Смотреть с любого устройства в броузере по ИП-адресу есп (хз как узнать без консоли (уарт-то подключен к ардуине). через админку роутера, лол)